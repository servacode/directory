import{readFile}from'node:fs/promises';const f=[];const req=[['apps/api/src/modules/ratings/rating.service.ts','ON CONFLICT(user_id,facility_id)'],['apps/api/src/modules/ratings/rating.service.ts','CANNOT_RATE_OWN_FACILITY'],['apps/api/src/modules/account/profile-image.service.ts','assertImageSignature'],['apps/api/src/storage/object-storage.service.ts','safePath'],['apps/api/src/modules/auth/core/user-account.service.ts','profileImageUrl']];for(const[x,p]of req){const t=await readFile(x,'utf8');if(!t.includes(p))f.push(`${x}: missing ${p}`)}if(f.length){console.error('Phase 10 verification failed\n'+f.join('\n'));process.exit(1)}console.log('Phase 10 static verification: PASS');console.log('Ratings: one row per user/facility');console.log('Own-facility rating prevention: PASS');console.log('Profile photo storage abstraction: PASS');
