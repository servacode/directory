import { resolve } from 'node:path';import{spawn}from'node:child_process';const url=process.env.DATABASE_URL;if(!url)throw new Error('DATABASE_URL is required');const file=resolve('infrastructure/database/seeds/001_core_reference_data.sql');const child=spawn('psql',[url,'-v','ON_ERROR_STOP=1','-f',file],{stdio:'inherit',shell:process.platform==='win32'});const code=await new Promise((resolveCode,reject)=>{child.on('error',reject);child.on('exit',resolveCode)});if(code!==0)throw new Error(`psql exited with ${code}`);console.log('Core seed complete.');
