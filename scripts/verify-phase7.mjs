import {readFile} from 'node:fs/promises';const failures=[];const req=[['apps/api/src/modules/availability/availability-engine.ts','TEMPORARILY_CLOSED'],['apps/api/src/modules/availability/availability-engine.ts','nextOpeningAfter'],['apps/api/src/modules/availability/time-zone.ts','Intl.DateTimeFormat'],['apps/api/src/modules/availability/temporary-closure.service.ts','TEMPORARY_CLOSURE_OVERLAP'],['apps/api/src/modules/availability/facility-availability.service.ts','APP_TIMEZONE']];for(const [f,p] of req){const t=await readFile(f,'utf8');if(!t.includes(p))failures.push(`${f}: missing ${p}`)}const db=await readFile('infrastructure/database/migrations/0004_hours_duty_ratings.sql','utf8');if(/is_open_now/i.test(db))failures.push('Database must not persist is_open_now truth');if(failures.length){console.error('Phase 7 verification failed\n'+failures.join('\n'));process.exit(1)}console.log('Phase 7 static verification: PASS');console.log('Availability source of truth: computed');console.log('Timezone: Asia/Damascus');
