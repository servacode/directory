# Phase 4 — Maps + Location + Geo

**Status:** source/core implemented; external native build verification pending.

- Device/manual location modes.
- PostGIS distance/bounds queries.
- Map provider abstraction.
- Facility pins consume server-provided category icon keys with a generic `store` fallback.
- Duty indicator is driven by pharmacy technical specialization/capability, not public category enumeration.
