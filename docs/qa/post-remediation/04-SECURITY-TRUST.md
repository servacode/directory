# Security / Trust Reassessment

**Score: 9.2/10 source — staging adversarial checks required.**

Closed findings include IDOR-sensitive re-verification rules, suspension bypass, Admin refresh-token exposure, proxy-aware throttling, HMAC OTPs, private evidence auditability, authorization-before-image-decode, safe image re-encode and enforced maintenance mode.

Password recovery remains OFF until its provider is configured. Evidence retention is now an executable, configurable policy.
