# Reference Data & Platform Settings — Phase 11

All Syrian governorates are seeded; activation is operational data. Public location endpoints expose ACTIVE geography only. Doctor specialties and nursing services are database-managed reference data. Platform settings use a typed registry: unknown keys and invalid values are rejected, and security secrets never enter this table. Critical feature flags are enforced by the backend, not just hidden in clients.
