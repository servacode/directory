# Public Discovery — Phase 9

Public discovery is governorate-wide, not a small-radius gate. All ACTIVE facilities in an ACTIVE governorate/city are eligible; precise coordinates only change ordering to nearest-first. Doctor discovery requires a specialty; pharmacy, nursing and medical-supplies categories do not require a child category. Nursing service remains an optional secondary filter. Availability is batch-loaded to avoid per-card N+1 calls.
