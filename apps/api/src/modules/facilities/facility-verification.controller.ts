import{Controller,Delete,Get,Param,Post,UploadedFile,UseGuards,UseInterceptors}from'@nestjs/common';import{FileInterceptor}from'@nestjs/platform-express';import{MAX_IMAGE_UPLOAD_BYTES}from'@health/config';import{parseIdentifier}from'@health/contracts';import type{AuthenticatedPrincipal}from'../auth/core/ports.js';import{AccessTokenGuard}from'../auth/http/access-token.guard.js';import{CurrentPrincipal}from'../auth/http/current-principal.decorator.js';import{FacilityVerificationService,type UploadedVerificationFile}from'./facility-verification.service.js';
@UseGuards(AccessTokenGuard)@Controller('facilities/:facilityId/verification')export class FacilityVerificationController{constructor(private readonly verification:FacilityVerificationService){}
@Get()summary(@CurrentPrincipal()p:AuthenticatedPrincipal,@Param('facilityId')facilityId:string){return this.verification.ownerSummary(p.userId,parseIdentifier(facilityId,'$.facilityId'))}
@Post(':requirementId/evidence')@UseInterceptors(FileInterceptor('file',{limits:{fileSize:MAX_IMAGE_UPLOAD_BYTES,files:1}}))upload(@CurrentPrincipal()p:AuthenticatedPrincipal,@Param('facilityId')facilityId:string,@Param('requirementId')requirementId:string,@UploadedFile()file:UploadedVerificationFile){return this.verification.upload(p.userId,parseIdentifier(facilityId,'$.facilityId'),parseIdentifier(requirementId,'$.requirementId'),file)}
@Delete('evidence/:evidenceId')remove(@CurrentPrincipal()p:AuthenticatedPrincipal,@Param('facilityId')facilityId:string,@Param('evidenceId')evidenceId:string){return this.verification.remove(p.userId,parseIdentifier(facilityId,'$.facilityId'),parseIdentifier(evidenceId,'$.evidenceId'))}
}
