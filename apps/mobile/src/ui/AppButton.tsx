import React from 'react';import { Pressable, StyleSheet, View } from 'react-native';import type { IconKey } from '@health/icon-registry';import { radius, semanticColors, spacing } from '@health/design-tokens';import { AppIcon } from './AppIcon.js';import { AppText } from './AppText.js';
export function AppButton({label,onPress,icon,variant='primary',disabled=false}:{label:string;onPress:()=>void;icon?:IconKey;variant?:'primary'|'secondary'|'ghost';disabled?:boolean}){const fg=variant==='primary'?semanticColors.surface:semanticColors.primary;return <Pressable accessibilityRole="button" accessibilityLabel={label} accessibilityState={{disabled}} disabled={disabled} onPress={onPress} style={({pressed})=>[styles.base,variant==='primary'?styles.primary:variant==='secondary'?styles.secondary:styles.ghost,(pressed||disabled)&&styles.dim]}><View style={styles.row}>{icon?<AppIcon name={icon} color={fg} size={20}/>:null}<AppText style={{color:fg,fontWeight:'700'}}>{label}</AppText></View></Pressable>}
const styles=StyleSheet.create({base:{minHeight:48,borderRadius:radius.md,paddingHorizontal:spacing.lg,alignItems:'center',justifyContent:'center'},primary:{backgroundColor:semanticColors.primary},secondary:{backgroundColor:semanticColors.primarySoft},ghost:{backgroundColor:'transparent'},dim:{opacity:.55},row:{flexDirection:'row-reverse',alignItems:'center',gap:spacing.sm}});
